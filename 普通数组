1.最大子数组和
如果前缀和 <= 0，直接把前面抛了

2.合并区间
先对 sort 写个 new comparator() 进行排序
第二个的左边比第一个右边小再进行判断
int[][] = list.toArray (new int[0][]) ；

3.轮转数组
额外数组 (i + k) % n
数组翻转 对应数组都翻转
环状替换

4.除自身以外数组的乘积
依旧左前缀 右后缀
实时更新数组 利用顺序操作，操作完一次不必再操作，可用数组记录值 临时变量

5.缺失的第一个正数
抽屉原理 找缺失的 1~n 都有 那就是 n+1
原地哈希 答案数组作为哈希表
