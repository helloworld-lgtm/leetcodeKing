1.移动零
类似于滑动窗口，遇到符合筛选条件的使right++，进行操作后left++
对于要找什么数，就用这个数特征的判断条件

2.盛水最多的容器
双指针类似俩索引，不是非得遍历寻找极限
小的指针从中头中尾

3.三数之和
双指针进行大小变化时注意越界问题，每次变化都要关注是否合理
 while (l < r) {
                int sum = nums[l] + nums[r];
                if (sum == target) {
                    res.add(Arrays.asList(nums[i], nums[l], nums[r]));
                    l++;
                    r--;
                    // 跳过重复元素
                    while (l < r && nums[l] == nums[l - 1]) l++;
                    while (l < r && nums[r] == nums[r + 1]) r--;
                } else if (sum < target) {
                    l++;
                } else {
                    r--;
                }
            }
三数之和固定一个数就变成两数之和 两数之和排序后当双指针处理

4.接雨水
count尽量只针对每一次计数，此题有优良算法
遍历一个数组左右最小值可以分别从左右遍历
一定明晰指针变动逻辑，是否全面、合理

双指针可以解决合并类问题 交替合并字符串
class Solution {
    public String mergeAlternately(String word1, String word2) {
        int m = word1.length(), n = word2.length();
        int i = 0, j = 0;

        StringBuilder ans = new StringBuilder();
        while (i < m || j < n) {
            if (i < m) {
                ans.append(word1.charAt(i));
                ++i;
            }
            if (j < n) {
                ans.append(word2.charAt(j));
                ++j;
            }
        }
        return ans.toString();
    }
}
